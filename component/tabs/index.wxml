<scroll-view wx:if="{{ scroll }}" scroll-x="true" class="wux-class wux-tabs wux-tabs--scroll">
    <slot></slot>
</scroll-view>
<view wx:if="{{ !scroll }}" class="wux-class wux-tabs" style="{{bgColor ? 'background: ' + bgColor : ''}}">
    <slot></slot>
</view>